version: '3.4'

services:
  eshop.modernized.webforms:
    image: eshop/modernizedwebformsltsc
    build:
      context: .\src\eShopModernizedWebForms
      dockerfile: Dockerfile

# sql.data:
#   image: microsoft/mssql-server-windows-developer